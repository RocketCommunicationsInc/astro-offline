'use strict';

const index = require('./index-7f00b6e7.js');

const FormFieldMessage = (props, children) => {
  const { helpText, errorText } = props;
  return ((helpText || errorText) && (index.h("div", { class: {
      'rux-error-text': !!errorText,
      'rux-help-text': !!helpText,
    } },
    children,
    helpText && !errorText && helpText,
    errorText && errorText)));
};

exports.FormFieldMessage = FormFieldMessage;

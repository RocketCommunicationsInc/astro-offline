'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-7f00b6e7.js');

const ruxIconCss = ":host{--icon-default-color:var(--color-primary);display:inline-flex;color:var(--icon-default-color)}:host([hidden]){display:none}svg,svg>svg{height:100%;width:auto}.icon{display:inline-flex}";

const RuxIcon = class {
  constructor(hostRef) {
    index.registerInstance(this, hostRef);
    // eslint-disable-next-line
    this.svg = '';
    /**
     * The size of the icon. Can be 'extra-small', 'small', 'normal', 'large', 'auto' or any custom value ('30px', '1rem', '3.321em')
     */
    this.size = 'normal';
  }
  get iconLabel() {
    if (this.label) {
      return this.label;
    }
    else {
      return this.icon;
    }
  }
  render() {
    const SVG = `rux-icon-${this.icon}`;
    return (index.h(index.Host, null, index.h(SVG, { class: "icon", size: this.size, title: this.iconLabel })));
  }
};
RuxIcon.style = ruxIconCss;

exports.rux_icon = RuxIcon;
